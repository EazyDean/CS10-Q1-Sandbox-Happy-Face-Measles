//Global Variables

void setup() {//End setup
size(400, 300);

buttonX = ;
buttonY = ;
buttonWidth = width*1/2
buttonHeight = height*1/2;
}

void draw() {}//End draw
rect(buttonX, buttonY, buttonWidth, buttonHeight);

void keyPressed() {
  //Quit Keyboard Button
  if (key == 'q' || key == 'Q') { 
    exit();
  }//End IF
}//End keypressed

void mousePressed() {
  if(mouseX>width*1/4 && mouseX<width*3/4 && mouseY>height*1/4 && exit) 
  
}//End mousePressed
